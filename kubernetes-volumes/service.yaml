apiVersion: v1
kind: Service
metadata:
  name: homework
  namespace: homework

spec:
  type: ClusterIP

  selector:
    app: homework

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
