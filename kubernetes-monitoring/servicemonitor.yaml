apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: nginx-monitoring
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
