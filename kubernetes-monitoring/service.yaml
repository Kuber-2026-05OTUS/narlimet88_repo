apiVersion: v1
kind: Service
metadata:
  name: nginx-monitoring
  labels:
    app: nginx-monitoring
spec:
  selector:
    app: nginx-monitoring
  ports:
    - name: http
      port: 80
      targetPort: http

    - name: metrics
      port: 9113
      targetPort: metrics
